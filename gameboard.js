export default class GameBoard{
	constructor(){
		this.addElement(element);
	}

	addElement(element){
		
		let elements = new Array(7);

			for (var i = 0; i < elements.lenght; i++) {
				elements[i] = Personnage, Arme;
			}
		elements.push(element);
	}
}



//creer tableau elements qui contient les éléments personnage et armes.
//creer une methode pour ajouter un élément dans le tableau elements et ajouter element dans le tableau elements.
